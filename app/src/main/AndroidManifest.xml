<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.solution2">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".app.MovieApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Design.NoActionBar">
        <activity android:name=".presentation.splash.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".presentation.popular.PopActivity"/>
        <activity android:name=".presentation.details.DetailsActivity"/>
        <activity android:name=".presentation.playing.NowPlayingActivity"/>
        <activity android:name=".presentation.choice.ChoiceActivity"/>
        <activity android:name=".presentation.upcoming.UpcomingActivity"/>
        <activity android:name=".presentation.search.SearchActivity"/>
    </application>

</manifest>